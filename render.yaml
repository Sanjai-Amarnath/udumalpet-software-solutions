services:
  - type: web
    name: udumalpet-solutions-api
    env: docker
    rootDir: backend
    plan: free
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: FRONTEND_ORIGIN
        sync: false

